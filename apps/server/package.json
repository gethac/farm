{
  "name": "@qq-classic-farm/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "postbuild": "node -e \"const { copyFileSync, mkdirSync } = require('node:fs'); mkdirSync('dist/src/db', { recursive: true }); copyFileSync('src/db/schema.sql', 'dist/src/db/schema.sql');\""
  },
  "dependencies": {
    "@qq-classic-farm/config": "file:../../packages/config",
    "@qq-classic-farm/protocol": "file:../../packages/protocol",
    "better-sqlite3": "^11.0.0",
    "fastify": "^5.0.0"
  }
}